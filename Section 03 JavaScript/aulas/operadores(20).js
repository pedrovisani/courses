// aritmeticos = + (adição e concatenação), -, /, ** (potenciação), % (resto da divisão), * (multiplicação) 
// aritmeticos = precedência = (), **, *, / e %, + e -

//contador++ (mostra, depois incrementa) ou ++contador (primeiro incrementa, depois mostra) = cada vez que é chamado acrescenta 1 ao seu valor
//contador-- (mostra, depois decrementa) ou --contador (primeiro decrementa, depois mostra) = cada vez que é chamado subtrai 1 ao seu valor

/* let contador = 1
   contador = contador + 1 (etc) 
   
   =
   
   contador += 2 (pode ser feito com qualquer operador aritmetico) (operadores de atribuição)

   parseInt(x) converte string em numero inteiro
   parseFloat(x) converte string em numero não inteiro
   Number(x) entende o tipo do numero de forma automática
   
   */