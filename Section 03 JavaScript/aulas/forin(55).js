const frutas = ['Pera', 'Maçã', 'Uva',];

// for (let i in frutas) { // lê índices
//     console.log(frutas[i]);
// }

const pessoa = {
    nome: 'Luiz',
    sobrenome: 'Otavio',
    idade: 30
};

 for (let i in pessoa) { // lê índices
     console.log(i, pessoa[i]);
 }

 // console.log(pessoa.nome); = console.log(pessoa['nome']);