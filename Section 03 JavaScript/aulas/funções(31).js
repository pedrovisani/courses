/*const raiz = function (n) // função anônima
{
    return n ** (1/2);
};

forma moderna de ser escrita:

const raiz = n => n ** 0.5; // arrow function

console.log(raiz(9)) */

////////////////////////////////////

/* function soma(x,y) // caso defina um valor x = 1, y = 1. Caso não haja entrada de numéro, as variáveis assumirão os valores definidos
{
    const resultado = Number(x + y);
    return resultado; // não executa nada abaixo de return
}

console.log(soma(2,9));
console.log(soma(2,8));
console.log(soma(2,10)); */

/* 
function saudacao(nome) {
    return `Bom dia, ${nome}!`;
}

const variavel = saudacao('Luiz');
console.log(variavel)*/

////////////////////////////////////

/* saudacao('Pedro');
saudacao('Maria');
saudacao('Luiza'); */